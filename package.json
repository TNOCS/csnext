{
  "private": true,
  "license": "MIT",
  "scripts": {
    "postinstall": "lerna bootstrap --hoist && lerna run link",
    "dev": "lerna run --parallel dev",
    "clean": "lerna run --parallel clean",
    "prebuild": "lerna run prebuild",
    "building": "lerna run build",
    "postbuild": "lerna run postbuild",
    "build": "yarn prebuild && yarn building && yarn postbuild"
  },
  "devDependencies": {
    "del-cli": "^1.1.0",
    "lerna": "^2.11.0"
  },
  "dependencies": {}
}
